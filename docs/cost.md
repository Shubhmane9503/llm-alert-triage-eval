# Cost methodology

Live-run cost is calculated from input/output token counts recorded for each
model call and an explicitly configured price snapshot. The report records the
price, currency, unit, source URL, and date. Historical runs are never
recalculated using a newer price.
